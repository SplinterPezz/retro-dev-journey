// Shared player config — used across multiple pages (Home, Login, Sandbox),
// not just within the sandbox game.
export const playerSpritePrefix: string = 'chef'; // prefix used to build player sprite filenames, e.g. {prefix}_walk_S.gif
export const playerName: string = 'Davide'; // name shown above the player in the sandbox
