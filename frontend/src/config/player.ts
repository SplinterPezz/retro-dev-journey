// Shared player config — used across multiple pages (Home, Login, Sandbox),
// not just within the sandbox game.
export const playerSpritePrefix: string = 'dude'; // prefix used to build player sprite filenames, e.g. {prefix}_walk_S.gif
export const playerName: string = 'Dude'; // name shown above the player in the sandbox
